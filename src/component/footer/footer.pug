include ../footer-pager/footer-pager

mixin footer(pager)
  .footer
    if pager
      +footerPager(5)
    .footer__menu
      each item in navFooter
        a.footer__nav(href= item.href)= item.title


